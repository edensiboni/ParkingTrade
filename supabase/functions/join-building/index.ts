// DELETED: join-building Edge Function has been removed as part of the Magic Login refactor.
// Users are now pre-registered by admins via direct profile creation (phone + apartment_id).
// The auth.users trigger (migration 014) links auth accounts to pre-created profiles on first login.
//
// This file is intentionally left as a stub. It can be safely deleted from the repo.
export {};
