* text=auto

# Shell scripts must stay LF so their `#!/bin/bash` shebang works under
# Git Bash / WSL after a Windows checkout (core.autocrlf would otherwise
# rewrite them to CRLF and break execution).
*.sh   text eol=lf

# Windows scripts want CRLF.
*.bat  text eol=crlf
*.ps1  text eol=crlf
